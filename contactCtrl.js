app.controller("contactCtrl", function ($scope,$http) {
	
	
   /* $http.get("get-contact.php",{ params: {'test':'test'}})
			.then(function (response) {
			console.log(response);
			$scope.contacts = response.data.contacts;
			}); */
   });